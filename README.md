# release-notes-api
Small public Django REST project: CRUD for tools and locations.  - Django ORM (models, relations, migrations) - Auth with HttpOnly session cookies, not JWT - PostgreSQL + Docker Compose - Tests for models and cookie login  Built to show real Django, not a JWT tutorial.
